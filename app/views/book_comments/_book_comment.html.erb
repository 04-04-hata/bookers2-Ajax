<% book.book_comments.each do |book_comment| %>
<table>
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <th colspan=""></th>
    </tr>
  </thead>
    <tbody>
    <td><%= link_to user_path(book_comment.user) do %>
     <%= attachment_image_tag book_comment.user,:profile_image,:fill,40,40,format:'jpeg',fallback:"no_image.jpeg" %>
     <%= book_comment.user.name %>
    <% end %>
    </td>
    <td><%= book_comment.comment %></td>
    <% if book_comment.user == current_user %>
      <td><%= link_to "Destroy",book_book_comment_path(book_comment.book,book_comment),method: :delete ,class:"btn btn-sm btn-danger"%></td>
    <% end %>
  <% end %>
  </tbody>
</table>
